<Page navigatingTo="onNavigatingTo" tap="onTap">
    <FlexboxLayout class="instructions-container">
        <StackLayout id="instructions">
            <Image src="{{ instructionsImage }}"/>
        </StackLayout>
        <FlexboxLayout class="ok-button">
            <Image src="{{ buttonImage }}"/>
        </FlexboxLayout>
    </FlexboxLayout>
</Page>
